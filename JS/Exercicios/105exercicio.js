let capturaClasse = document.querySelector(".resultado");
let max = Number(prompt("Quantos palavras voce quer comparar"));
let array = [];



function opa(params) { let maior = 0
    for (let i = 0; i < max; i++) { 
        let contador = 0
       palvras=prompt(`Escreva sua ${i}° palavra `)
       if (palavras.length>maior) { 
        palavras.length=maior
        
       }
        palavras.push(array)
       
        
    }
 




    
}

capturaClasse.innerHTML = opa(array);

