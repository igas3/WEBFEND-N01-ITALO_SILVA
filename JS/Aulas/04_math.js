let n1 = 82;
let n2 = -4;

console.log(Math.abs(n2)); // Transforma em número absoluto

console.log(Math.ceil(3.14)); // Arrendonda pra cima

console.log(Math.floor(3.8)); // Arrendoda para baixo

console.log(Math.round(3.5)); // Arrendoda para o número mais próximo

let nota1 = 10;
let nota2 = 7;
let nota3 = 6;

console.log(Math.max(nota1, nota2, nota3)); // Retorna o maior valor

console.log(Math.min(nota1, nota2, nota3)); // Retorna o menor valor

console.log(Math.pow(2,3)); // O primeiro valor é a BASE e o sugundo é o EXPOENTE

console.log(Math.sqrt(9));//Retorna a Raiz quadrado

console.log(Math.log(2)); // Retorna o logaritimo natural

console.log(Math.sin(2)); // Calcula o Seno
console.log(Math.cos(2)); // Calcula o coseno
console.log(Math.tan(2)); // Calcula a tangente

let dinheiro = 7.578;
console.log(dinheiro.toFixed(2)); // Determina a quantidade de casas decimais
console.log(dinheiro.toFixed(5));

const pi = Math.PI;
console.log(pi);

const euler = Math.E;
console.log(euler);

let numAle = Math.random() * 21

console.log(Math.floor(numAle))

